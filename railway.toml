# Railway Configuration for OmniAgent Backend
# Using Dockerfile builder for deployment

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
context = "."

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
