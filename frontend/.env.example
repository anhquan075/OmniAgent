# OmniAgent Casper frontend

VITE_DEFAULT_NETWORK=casper-test
VITE_CASPER_EXPLORER_URL=https://testnet.cspr.live

# Browser requests stay same-origin at /api. Vite dev uses this as its proxy target.
VITE_API_URL=http://localhost:8000

# Production Node server proxy target. Railway can override this per service.
BACKEND_URL=
